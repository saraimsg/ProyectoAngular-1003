import { ProductoFilterPipe } from './producto-filter.pipe';

describe('ProductoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
